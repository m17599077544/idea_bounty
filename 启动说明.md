# 🚀 Idea Bounty 启动说明

## 环境要求

- **Node.js** 18 或以上（[下载](https://nodejs.org)）
- **npm**（随 Node.js 一起安装）

## 快速开始

### 1. 进入项目目录

```bash
cd "D:\Idea Bounty"
```

### 2. 安装依赖（首次运行需要）

```bash
npm install
```

### 3. 配置 API Key

编辑项目根目录的 `.env` 文件，填入你的 DeepSeek API Key：

```
OPENAI_API_KEY=sk-你的key
```

### 4. 启动开发服务器

```bash
npm run dev
```

启动后浏览器打开：

| 页面 | 地址 |
|------|------|
| 用户前台（提交点子） | http://localhost:3000 |
| 管理后台 | http://localhost:3000/admin |

### 5. 构建生产版本（如需部署）

```bash
npm run build
npm start
```

## 常用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器（热更新） |
| `npm run build` | 生产构建 |
| `npm start` | 启动生产服务器 |
| `npm run lint` | 代码检查 |

## 项目结构

```
D:\Idea Bounty\
├── src/
│   ├── app/
│   │   ├── page.tsx          # 用户首页
│   │   ├── admin/page.tsx    # 管理后台
│   │   └── api/              # 后端 API
│   ├── components/           # React 组件
│   └── lib/                  # 核心逻辑
│       ├── ai.ts             # AI 评估 + 查重
│       ├── db.ts             # SQLite 数据库
│       └── utils.ts          # 工具函数
├── data/                     # SQLite 数据库文件
├── .env                      # API Key 配置
└── package.json
```

## 技术栈

- **框架**：Next.js 16 (App Router)
- **语言**：TypeScript
- **样式**：Tailwind CSS
- **数据库**：SQLite (better-sqlite3)
- **AI**：DeepSeek API (deepseek-v4-pro)

## 常见问题

**Q: 启动后页面打不开？**
A: 检查终端是否显示 `✓ Ready`，确认端口 3000 没有被其他程序占用。

**Q: 提交点子报错？**
A: 检查 `.env` 文件中的 `OPENAI_API_KEY` 是否正确配置。

**Q: 评估很慢？**
A: deepseek-v4-pro 模型推理需要约 20-30 秒，这是正常现象。

**Q: 想清空数据库重新开始？**
A: 删除 `data/ideabounty.db` 文件后重启即可。
